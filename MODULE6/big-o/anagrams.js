// sorting takes O(n log(n)) time... 
// a complexity we haven't discussed yet


// time O(n*log(n))
// space O(1)
function slowAnagrams(str1, str2) {
}

// time O(n)
// space ---- doesn't matter 
function betterSpeedNotSoHotSpace (str1, str2) {
}

// time O(n)
// space - O(1)
function bestSpeedAndSpace (str1, str2) {
}

