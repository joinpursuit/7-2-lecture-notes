-- create a pokemon database, call it pokemon_dev

-- Create the following tables in pokemon_dev
    -- pokemon
        -- id
        -- name
        -- trainerId 
    -- trainers
        -- id 
        -- name

-- insert the data that we see in tables.png

-- the fun part
-- select all the pokemon 
-- select all the trainers 
-- select the id of a trainer called "Ash"
-- using the result from last query, select all the pokemon where the trainer is "Ash"
-- select all pokemon with the name of "Pikachu" 
-- using the result from the last query, select the name of all trainers that own a Pikachu
